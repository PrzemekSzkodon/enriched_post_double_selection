# enriched_post_double_selection
We seek to improve the PDS method (which relies on linear relationships) by adding non-linear variables via a systematic additive dictionary approach. We aim to enrich the list available to PDS by using NNMR and PySR to build relevant non-linear variables.
